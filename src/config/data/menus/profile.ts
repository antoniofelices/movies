import type { RoutesProps } from '@/types/interfaces'

const user: RoutesProps[] = [
    {
        id: 1,
        url: 'profile',
        text: 'Profile',
        orderMenu: 1,
    },
]

export default user
